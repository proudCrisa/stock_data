## 1. A. Collector Genesis, No-Follow Identity, And Finalized Append-Only Evidence

- [x] 1.1 Add `stockdata/collector_continuity.py` with frozen schema constants and exact canonical encoders for collector genesis, physical file identities, logical state, ledger events, and continuity closure; reject optional or unknown fields.
- [x] 1.2 Implement root-to-leaf no-follow opening for the canonical parent, database, and ledger; require regular final files and bind parent/file device and inode without using mutable size or timestamp metadata.
- [x] 1.3 Extend future collector preparation to exclusively create the ledger and SQLite file, generate and persist one immutable 256-bit database UUID, cross-bind one ledger genesis, and fsync both artifacts and their containing directory.
- [x] 1.4 Add exact collector connection handling with no automatic migration, `journal_mode=DELETE`, `synchronous=FULL`, foreign keys, bounded busy timeout, and fail-closed WAL/SHM detection.
- [x] 1.5 Install and verify immutable genesis/cohort/evidence triggers, finalized-price insert-or-identical-no-op behavior, and monotonic fixed-identity sync coverage without weakening non-collector research databases.
- [x] 1.6 Add focused tests for normal inode stability, database UUID stability, same-path replacement, parent/final symlinks, non-regular files, same-inode rollback, finalized-price overwrite/delete, identical retry, and sync-coverage shrink/identity drift.

## 2. B. Canonical Ledger, Lock, State Machine, Postconditions, And Crash Recovery

- [x] 2.1 Implement the exact canonical JSONL event parser and append writer for genesis, registration binding, attempt start, attempt completion/failure, and controlled SQLite recovery; verify sequence and full hash-chain closure on every read.
- [x] 2.2 Implement streaming `collector_state_sha256` over exact primary-key-ordered rows and counts for genesis, cohort, daily, receipts, sync coverage, context, universe, status, and corporate-action tables in one read-only consistent transaction.
- [x] 2.3 Implement a non-blocking exclusive ledger `flock` held across a complete phase and normally duplicated into child processes; make the primitive prove only bound ledger identity and current held-lock state, and reject competing capture/materialization before any append or provider call.
- [x] 2.4 Freeze `stockdata-forward-collector-step-state/1` with aggregate hash, exact ten-table counts and table digests, exact allowed-table selector-complement digests, and receipt high-water in one task 2.2-typed snapshot; freeze the four stable step IDs, global ordinals `0..11`, exact canonical command hashes, allowed table/date/phase/cohort/source deltas, non-final daily insert rejection, exact-coverage no-retimestamp behavior, and independent raw postcondition verifiers.
- [x] 2.5 Generate a fresh 256-bit nonce per attempt, persist and fsync its mandatory `lease_nonce_sha256` and complete consistent `step_state_before` in `ATTEMPT_STARTED` before child execution, transfer the raw nonce exactly once through an inherited anonymous pipe with the duplicated lock descriptor, and require the child to match the exact active tail and nonce before any provider call or write transaction; after execution reverify path identity, UUID, chain, complete step state, return code, stdout/stderr digests, and raw postconditions before appending exactly one terminal event with mandatory consistent `step_state_after` that invalidates the nonce.
- [x] 2.6 Implement dangling-attempt recovery from the persisted complete `step_state_before`, per-table and selector-complement digests, receipt high-water, and current raw rows for unchanged state, independently complete atomic state, verified partial price state, hot rollback-journal recovery, and forbidden drift; preserve history, persist complete consistent terminal `step_state_after`, and allocate a new attempt ID for every retry.
- [x] 2.7 Add deterministic tests for line tampering, truncation, duplicate sequence, wrong previous hash, forged or inconsistent step-state terminal events, aggregate/count collisions caught by table or selector-complement digests, foreign same-table evidence, non-final daily insert, exact-coverage retry preserving `retrieved_at`, receipt high-water attribution/orphans, exact command/ordinal drift, stdout-only success, zero exit with missing evidence, nonzero exit, lock inheritance/contention, self-locked descriptors without the nonce preimage, missing/wrong/truncated/overlong/reused nonce pipes, descriptor and nonce non-leakage, and direct-writer bypass.
- [x] 2.8 Add crash-boundary tests before child start, during context/action transactions, between per-symbol price commits, after child commit before terminal append, and during rollback-journal recovery; assert only classified monotonic recovery is retryable.

## 3. C. Registration `/4` And Registered Capture Orchestration

- [x] 3.1 Advance collector capability and registration schemas, include database/ledger/genesis identities in `prerequisites.collector`, and make `/4` the sole accepted prospective capture schema.
- [x] 3.2 Bind the canonical registration SHA-256, panel, sessions, and prerequisite hash in exactly one `REGISTRATION_BOUND` event; support only byte-identical completion after a crash between exclusive registration write and ledger bind.
- [x] 3.3 Route registered capture through the continuity lock and total order across all three sessions and four steps per session; reject wrong day/window, duplicate phase, skipped predecessor, quarantined registration, static-prerequisite drift, and current-state drift before any provider request.
- [x] 3.4 Update CLI parameter plumbing and child lease validation while preserving existing behavior for databases without collector genesis; ensure standalone collector-marked forward commands fail before a write transaction.
- [x] 3.5 Add registration/capture tests for clean `/4`, registration-bind crash recovery, different existing bytes, duplicate binding, `/3` and earlier rejection, database/ledger replacement, wrong order/date, duplicate phase, cross-date gaps, and zero provider calls on preflight failure.
- [x] 3.6 Update the active provider-readiness registration fixtures to use `/4` without marking the expired `/3` panel or any external trust/data blocker complete.

## 4. D. Consistent Materialization And Continuity-Gated Provider Bundle Input `/2`

- [x] 4.1 Acquire the collector continuity lock before materialization, require the complete registered schedule and matching logical tail, and replace raw live-database byte reads with a consistent SQLite backup snapshot.
- [x] 4.2 Build and freeze `stockdata-forward-collector-continuity-closure/1` over live physical identity, database UUID, registration hash, ledger head, logical state, and the immutable snapshot database reference.
- [x] 4.3 Split the local provider bundle input schema from the RQGM-facing export envelope, advance bundle input to `/2`, and require registration, ledger snapshot, and continuity-closure locators while keeping `stockdata-rqgm-provider-export/1` stable.
- [x] 4.4 Reverify registration, full ledger chain and schedule, UUID, snapshot logical state, and closure during every read-only provider export; reject missing, legacy, malformed, drifted, duplicate, or mismatched continuity artifacts.
- [x] 4.5 Prove continuity is a negative provenance gate only: it can reject materialization/export but cannot populate a component, admit signed authority, satisfy availability, change a component boolean, or turn an otherwise blocked nine-component report ready.
- [x] 4.6 Add materializer/export tests for capture lock contention, database change during snapshot, WAL/SHM presence, bundle `/1` input rejection, closure tampering, ledger rollback, UUID/state mismatch, valid continuity plus missing evidence, and independently complete positive fixtures.

## 5. E. RQGM Fail-Closed Compatibility And Two-Repository Verification

- [x] 5.1 Add frozen `stock_data` provider bundle `/2` fixtures for valid-blocked, valid-complete, legacy, missing, malformed, truncated-ledger, registration-drift, state-rollback, and forged-continuity cases.
- [x] 5.2 Update `super-trader-rqgm` provider-export compatibility fixtures to consume the unchanged verified envelope, reject any caller-supplied continuity token or unverifiable provider path, and preserve candidate-reader zero-call behavior on provider failure.
- [x] 5.3 Run the focused `stock_data` genesis, ledger, crash, registration, capture, materializer, export, and readiness suites with all new negative scenarios passing.
- [x] 5.4 Run the full `stock_data` pytest, compile, Ruff, strict OpenSpec validation, and `git diff --check` in its supported local environment with no network or database/artifact mutation outside test temporaries.
- [x] 5.5 Run the focused and full `super-trader-rqgm` provider-export, Judge receipt, authority snapshot, and paper-kernel regressions plus compile, Ruff, strict OpenSpec validation, and `git diff --check`.
- [x] 5.6 Recompute the real local readiness path read-only and record that continuity does not remove production trust-root/signer, signed authority, prospective future-panel, immutable Judge, economic evidence, or release-authorization blockers.
- [x] 5.7 Obtain a final Sol read-only P0/P1 review covering no-follow/TOCTOU, in-place rollback, ledger recovery, lease bypass, SQLite snapshot consistency, readiness non-authority, `/3` rejection, bundle `/2`, and RQGM fail-closed compatibility; resolve every finding through Terra implementation and Luna regression before considering apply complete.
